--- a/Makefile.am
+++ b/Makefile.am
@@ -26,14 +26,14 @@
 	cp -r -t "$(distdir)" $(installFiles)
 
 install-exec-hook:
-	mkdir -p "$(prefix)/$(productID)"
-	$(INSTALL) $(installFiles) "$(prefix)/$(productID)"
+	mkdir -p "$(DESTDIR)/usr/games"
+	$(INSTALL) $(installFiles) "$(DESTDIR)/usr/games"
 
 install-data-local:
-	cp -r -t "$(prefix)/$(productID)" $(installData)
-	mkdir -p "$(prefix)/$(productID)/Songs"
+	mkdir -p "$(DESTDIR)/usr/share/games/stepmania/Songs"
+	cp -r -t "$(DESTDIR)/usr/share/games/stepmania" $(installData)
 
 uninstall-hook:
-	rm -f "$(prefix)/$(productID)/stepmania"
-	rm -f "$(prefix)/$(productID)/GtkModule.so"
+	rm -f "$(DESTDIR)/$(prefix)/stepmania"
+	rm -f "$(DESTDIR)/$(prefix)/GtkModule.so"
 # todo: properly remove data
